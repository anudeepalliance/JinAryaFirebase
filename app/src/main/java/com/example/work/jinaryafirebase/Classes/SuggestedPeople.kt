package com.example.work.jinaryafirebase.Classes

import android.net.Uri

class SuggestedPeople (var name : String, var photoUri : Uri,
                       var uid : String, var mutualConnections : Int)